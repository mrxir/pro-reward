# Uncomment the next line to define a global platform for your project
 
platform :ios, '10.0'

target 'reward' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for reward

  target 'rewardTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'rewardUITests' do
    # Pods for testing
  end

end
